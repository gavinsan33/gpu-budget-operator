/bin/
cover.out
*.test
.claude/*

# Generated by `make catalog-render` - requires BUNDLE_IMG to already be
# pushed to render correctly, so (unlike bundle/manifests's CRD copy) this
# would go stale/wrong the moment it's committed rather than re-rendered.
catalog/gpu-budget-operator/catalog.yaml
