apiVersion: apps/v1
kind: Deployment
metadata:
  name: gpu-quota-operator-controller-manager
  namespace: gpu-quota-operator-system
  labels:
    control-plane: controller-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      control-plane: controller-manager
  template:
    metadata:
      labels:
        control-plane: controller-manager
    spec:
      serviceAccountName: gpu-quota-operator-controller-manager
      containers:
      - name: manager
        # Resolves to whatever build.yaml's BuildConfig most recently built
        # into this ImageStreamTag - see the Makefile's `build-image` target.
        # `oc rollout restart` after a build is required to actually pick up
        # a new image, since plain apps/v1 Deployments (unlike
        # DeploymentConfigs) have no ImageChange trigger of their own.
        image: image-registry.openshift-image-registry.svc:5000/gpu-quota-operator-system/gpu-quota-operator:latest
        imagePullPolicy: Always
        command:
        - /manager
        args:
        - --leader-elect
        # The one, cluster-wide Prometheus every GpuQuota is evaluated
        # against - there is no per-namespace override. Auth (Bearer token)
        # and TLS trust (the mounted CA bundle below, or the system trust
        # store if absent) are always automatic, with no flag to change
        # either. This default targets OpenShift's built-in monitoring
        # stack, which requires the cluster-monitoring-view
        # ClusterRoleBinding (../bootstrap/monitoring_rolebinding.yaml).
        - --prometheus-url=https://thanos-querier.openshift-monitoring.svc:9091
        volumeMounts:
        - name: service-ca
          mountPath: /etc/gpu-quota-operator/service-ca
          readOnly: true
        resources:
          limits:
            cpu: 500m
            memory: 256Mi
          requests:
            cpu: 100m
            memory: 128Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          runAsNonRoot: true
      volumes:
      - name: service-ca
        configMap:
          name: gpu-quota-operator-service-ca
      securityContext:
        runAsNonRoot: true
